# iCare → GCI migration — FINAL SCOPE (PREPARED, NOT EXECUTED)

**Only the 3 internal tasks move (`executive_tasks`). Projects (19), projectFollowUps (6) and history (21) are not migrated.**
Read `FINAL_SCOPE_2026-09.md` first (numbers, the 3 tasks, guards, order). Code switch: `INTERNAL_TASKS_SWITCH_PLAN.md`.

| file | purpose |
|---|---|
| `00_dryrun.sql` | read-only: **D1** summary (tasks + the sizes that are guarded + target-table counts), **D2** the 3 tasks with what each becomes |
| `10_apply_batch.sql` | tasks-only apply. `execution_approved = false` by default; guards: pre-state (6 / 0 / 1), source sizes (3 / 19 / 6 / 21), migrate 3, duplicate 0, cleared due dates 2, content shape, `executive_tasks` 6 → 9, `crm_projects` and `crm_followups` unchanged |
| `20_rollback_batch.sql` | remove one batch (paste its id); refuses if migrated tasks were edited afterwards |
| `../../migrations/20260921000200_icare_tasks_schema.sql` | tasks-only schema (+ `../../rollbacks/20260921000200_icare_tasks_schema.rollback.sql`); touches only `executive_tasks` and `data_migration_batches` |
| `FINAL_SCOPE_2026-09.md` · `INTERNAL_TASKS_SWITCH_PLAN.md` · `QUICK_API_REMOVAL_PLAN.md` | scope / expected results · the Internal Tasks code plan · how the shell stops calling quick-api + the final blocker list |

`00_dryrun.sql`, `10_apply_batch.sql`, `20_rollback_batch.sql` are generated by `tools/build-icare-sql.mjs` (`node tools/build-icare-sql.mjs`) from one rule set; do not hand-edit one without the others.

Projects, follow-ups, manual customer mapping and any `crm_*` schema extension are not part of the plan (dropped by Chris, 2026-09).
